# Core dependencies
# 核心依赖

# No external dependencies required
# 无需外部依赖

# Optional dependencies
# 可选依赖
# sqlite3  # Built-in Python library | Python 内置库

# Development dependencies
# 开发依赖
pytest>=7.0.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
